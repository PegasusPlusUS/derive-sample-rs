use derive_sample_rs::MyTrait;

derive_sample_rs::MyTrait!(MyStruct);

struct MyStruct;

fn main() {
    //println!("i8::one() = {}", i8::one());
    //println!("i16::one() = {}", i16::one());
    //println!("i32::one() = {}", i32::one());
    //println!("MyStruct::one() = {}", MyStruct::one());
}

fn xmain() {
    println!("Hello, world!");
}
